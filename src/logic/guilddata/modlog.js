function updateModlogTimeForMany(session, guilds) {
  // TODO make modlog update system
  /*
  1. map guilds into different queries per flag
  2. add queries to session
  */
  console.log("Updating modlog for", guilds);
}

module.exports = {
  updateModlogTimeForMany
};
